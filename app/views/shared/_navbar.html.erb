<div class="py-6 px-4 bg-white/80 shadow-sm fixed min-w-6xl top-4 z-50 mb-6 rounded-2xl dark:bg-slate-800/80 backdrop-blur-sm border border-slate-300 dark:border-slate-700">
  <nav class="">
    <div class="mx-auto max-w-7xl flex items-center grid-cols-2 justify-between">
      <div class="">
        <%= link_to "Launchpad", root_path, class: "text-3xl font-bold bg-gradient-to-r from-purple-500 via-pink-500 to-yellow-500 text-transparent bg-clip-text" %>
      </div>
      <div class="flex flex-row gap-4 justify-end">
        <!-- FIX: Current.user doesn't work, gotta see why -->
        <% if not Current.user %>
          <%= link_to "Manage your offers", dashboard_path, class: "rounded-full px-4.5 py-2.5 hover:bg-slate-200 dark:hover:bg-slate-900 block font-bold text-center" %>
        <% else %>
          <%= link_to new_session_path, class: "rounded-full px-4.5 py-2.5 hover:bg-slate-200 dark:hover:bg-slate-900 block font-bold text-center" do %>
            <span class="mr-2">👋</span> Sign in
          <% end %>
        <% end %>
        <div data-controller="dark-mode">
          <button class="rounded-full px-3.5 py-2.5 hover:bg-slate-200 dark:hover:bg-slate-900 text-white block font-medium w-12 cursor-pointer" data-action="dark-mode#toggle">
            <span data-dark-mode-target="icon"></span>
          </button>
        </div>
      </div>
    </div>
  </nav>
</div>
